import { PlayerPropertyProvider } from './player-property-provider';

describe('PlayerPropertyProvider', () => {
  it('should create an instance', () => {
    expect(new PlayerPropertyProvider([])).toBeTruthy();
  });
});
